/target
# Reference sources tracked as submodules — do NOT ignore
# /references
fuzz/target
fuzz/artifacts
.DS_Store
bench_c_decode_linux
crates/libjpeg-turbo-rs-wasm/pkg/
# Claude Code agent runtime artifacts (worktrees, state, playwright cache)
.claude/worktrees/
.claude/agents/.omc/
.omc/
.playwright-mcp/
/state/
# Generated test fixtures (regenerated by the `test-corpus` workflow) and
# the cargo-fuzz local lockfile. Both are otherwise permanently untracked,
# which kept Codex review's auto-scope stuck on the working tree instead of
# the actual branch diff against origin/main.
/tests/corpus/
fuzz/Cargo.lock
