# `unsafe` Inventory — C-ABI crate (`libjpeg-turbo-rs-capi`)

> **Gated.** `tests/unsafe_inventory_gate.rs` lexes every `.rs` under
> `crates/libjpeg-turbo-rs-capi/src/`, attributes each code `unsafe` token to
> its enclosing item, and fails unless this file lists exactly those items
> with exactly those counts and every cell filled. Adding, removing or moving
> an `unsafe` therefore fails CI until the row here changes — the inventory
> edit is part of the pull-request diff, and `.github/CODEOWNERS` routes that
> diff to the maintainer. **Edit this file, never the gate, when a count
> moves.**

**Why an inventory and not a count** (P4-141 criterion 4, GitHub
[#480](https://github.com/developer0hye/libjpeg-turbo-rs/issues/480)). A raw
count says nothing about risk, and nowhere less than here: this crate is
`extern "C"` end to end, so *every* entry point is an `unsafe fn` by
construction and the declaration sites alone would inflate any total. What
matters per site is *why* safe Rust cannot express it, *which invariant*
makes it sound, *whether a safe caller can reach it*, *which test* would
catch the invariant breaking, *which tools* execute it, and *who last read
it*. That is the row. The companion document for the root crate is
[`UNSAFE_INVENTORY.md`](UNSAFE_INVENTORY.md).

## Scope and units

- **Scope:** `crates/libjpeg-turbo-rs-capi/src/` — the TurboJPEG 3 surface,
  its TurboJPEG 1.x/2.x legacy aliases, the libc allocation helpers, the
  `jpeg_memory_mgr` port and the mozjpeg-compat stubs. **One file is
  deferred:** `jpeglib.rs`, the classic `jpeg_*` surface — 501 sites in
  12.9k lines, 64 % of the crate's total. It is named in the gate's
  `DEFERRED` list rather than in prose alone, so it cannot be forgotten: the
  gate requires the path to exist and to still hold `unsafe`, and any *other*
  uninventoried source still fails. Closing it is the remaining work of
  criterion 4, tracked in `docs/last_mile/phase4.md` under P4-141.
- **Site:** one code `unsafe` token — outside comments, strings and
  literals — as found by the lexer in `tests/helpers/unsafe_scan.rs`. Under
  the crate's `#![deny(unsafe_op_in_unsafe_fn)]`, an `unsafe fn` and the
  `unsafe { … }` block inside it are **two** sites; that is deliberate, so
  the declaration (the contract) and the operation (the risk) are counted
  apart. For an `extern "C"` entry point the declaration site is the more
  interesting of the two: it is where the C caller's obligation is written
  down, and nothing in the body can check it.
- **Item:** the row key — the enclosing `fn` (the nearest declaration at or
  above the site), or the header for `unsafe impl` / `unsafe trait`. A site
  at module level, including one inside a `type` alias or a `static`
  initialiser, keys `(module)`. Attribution reads a comment-masked copy of
  the source, so neither a commented-out `fn` nor the phrase `unsafe impl …`
  in a trailing comment can name a row. It is a deterministic attribution,
  not a parser: the gate's failure message prints the exact rows it expects.
- **Sections** are one per file, `## \`path\` — N sites`, in path order; rows
  within a section are in first-occurrence order. Sites inside `#[cfg(test)]`
  code are inventoried too and say **test-only** in *Safe caller* — a test
  that dereferences a raw pointer is still code ASan needs to see.
- **The `---` rule below is the boundary.** Everything above it is this
  preamble, which explains the format with its own `##` headings and its own
  table and carries no inventory data. Everything below it is read by the
  gate, where a `## ` heading that is not `## \`path\` — N sites` is an error
  rather than prose.

## Columns

| Column | What goes in it |
|---|---|
| Item | The enclosing item, in backticks. Machine-checked. |
| Sites | Code `unsafe` tokens attributed to the item. Machine-checked. |
| Why not safe Rust | The concrete reason: an `extern "C"` declaration whose caller is C, a raw pointer plus a length the caller asserts, `Box::from_raw` reclaiming a handle, a `#[repr(C)]` vtable of `unsafe extern "C" fn`. |
| Invariant | What must hold — bounds, lifetime, aliasing, allocator provenance — and **where it is established**: which argument check, which `ImageLayout`/`checked_span` call, or "the caller's contract, unchecked" when nothing in the process can verify it. |
| Safe caller | `no: C ABI` for an exported entry point whose obligations belong to the C caller; `yes: via …` when 100 % safe Rust in this workspace can reach it; `test-only`. |
| Regression test | The test(s) that would fail if the invariant broke, by function name and file. `**none** — …` is an honest answer and a gap to file. |
| Tool coverage | Which CI legs execute the site (vocabulary below), and — just as important — which do not. |
| Reviewer | Who last read the code against the row, and when. |

**What the gate enforces, and what it does not.** It enforces that every site
is *listed*, that each row's count is exact, and that no cell is empty or a
template — so an added, removed or moved `unsafe` cannot merge without an
inventory edit in the same diff. It cannot check that a justification is
*true*. Content is a review question, which is what `.github/CODEOWNERS` is
for.

**A note this crate needs and the root crate does not.** *Safe caller* reads
`no: C ABI` on nearly every row, and that is a statement about Rust's
`unsafe` keyword, not about reachability: these symbols are exported from a
cdylib and any C program in the world can call them with any pointer it
likes. The obligation is real, it is simply not one the compiler can place on
anybody. What the `unsafe fn` marking buys is that a *Rust* consumer of this
crate's `rlib` cannot reach them without acknowledging it — the point P4-137
made when it removed a safe wrapper that implied otherwise.

## Tool-coverage vocabulary

The legs, as configured on 2026-09-08 (`.github/workflows/`). Note how few of
them reach this crate: `cross-arch.yml` runs `cargo test --tests` and
`wasm.yml` runs `cargo test --target wasm32-wasip1`, both of which select the
*root* package only, and `full-c-parity.yml` names root `--test` targets.
`armv7.yml` is the one architecture leg that names this crate, and it names
three things.

- **Integration Tests** — `ci.yml` *Integration Tests* (ubuntu x86_64) and
  its *(oracle 3.2.0)* twin. The step `Complete C-ABI integration inventory
  (P4-175)` runs `cargo test -p libjpeg-turbo-rs-capi --tests --features png
  --no-fail-fast`, i.e. **every** C-ABI integration suite, including files
  added since; the named focused steps around it are earlier signals on the
  same suites. This is the primary leg for everything in this document.
- **C-ABI unit tests** — the same job's `cargo test -p libjpeg-turbo-rs-capi
  --lib`: the `#[cfg(test)]` modules *inside* these sources, on the stable
  toolchain, uninstrumented.
- **ASan / UBSan** — `sanitizers.yml`: `cargo +nightly test --workspace
  --lib` with `-Z sanitizer=address` / `-Z ub-checks=yes`. `--lib`, so it
  covers this crate's own unit tests and **no integration suite**.
- **C-boundary ASan** — `sanitizers.yml`'s third job: a C harness compiled
  with `-fsanitize=address,undefined` `dlopen`s the ASan-instrumented cdylib
  and drives `tj3Init → tj3DecompressHeader → tj3Decompress8 → tj3Destroy`
  (plus `tj3Get`) over three fixtures — baseline, progressive and
  arithmetic. The only leg where a real C caller crosses this boundary, and
  it reaches five entry points. It allocates its own buffers with `malloc` /
  `free`, so the shared-allocator contract `tj3Alloc` / `tj3Free` exist for
  is **not** exercised here.
- **Miri** — `ci.yml`: `cargo miri test -p libjpeg-turbo-rs-capi --test
  capi_create_abi_guards --test capi_thread_affinity`. Two suites, both on
  the classic `jpeg_*` surface. **No TurboJPEG entry point in this document
  runs under Miri**, so no row may claim it.
- **ARMv7 (32-bit, qemu)** — `armv7.yml`: `-p libjpeg-turbo-rs-capi --lib`
  plus `capi_layout_adoption` and `capi_span_overflow_guards`, under
  `-C overflow-checks=on`. The leg where a 32-bit `usize` wrap in a span
  becomes a loud failure.
- **Windows / CAPI ABI Checks** — `ci.yml` builds every workspace test target
  (`--no-run`) on Windows and runs `format_message` there; *CAPI ABI Checks*
  (ubuntu, macos, windows) runs `abi_offsets` and `symbol_inventory` in
  release.
- **Stock-tool link** — `tests/capi_stock_tool_link.rs` in the **root** crate
  (`stock_tools_link_against_our_shim`) builds upstream `djpeg`/`cjpeg`/
  `jpegtran` against the shim and diffs their output against the system
  binaries, so it drives the memory manager the way libjpeg itself does. It
  runs wherever the root crate's `cargo test --tests` runs — Integration
  Tests, C Interop (macos aarch64) and the `cross-arch.yml` legs — and skips
  without a C compiler or the stock tools.
- **Not covered anywhere:** `cross-arch.yml` and `wasm.yml` (root package
  only), `full-c-parity.yml` (root `--test` targets), and Fuzz Smoke — all
  twelve fuzz targets drive the Rust API, so **no fuzzer reaches the C ABI**.
  That is criterion 3 of P4-141, still open.

## Maintaining a row

1. Run `cargo test --test unsafe_inventory_gate`. The failure names the file,
   the item and the count it found, with the source lines, and prints a
   paste-ready section for a file that is new here.
2. Re-read the code, not just the diff: a changed count means the contract
   may have changed. Update every cell the change touches, and the
   *Reviewer* cell always.
3. If the honest *Regression test* cell is `**none**`, add it to
   [P4-191](https://github.com/developer0hye/libjpeg-turbo-rs/issues/609) —
   the item that collects them — before the pull request merges, rather than
   opening a sibling.

---

## `crates/libjpeg-turbo-rs-capi/src/alloc.rs` — 9 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `libc_malloc` | 1 | Calls the libc `malloc` declared in this file's `extern "C"` block. It cannot be Rust's global allocator: C callers routinely release TurboJPEG output with their own `free()` (Pillow does exactly that for `tj3Compress8`'s `jpegBuf`), so the two pools must be the same one. | SAFETY comment: "`malloc` is a standard C function; we check for NULL before use and ensure the same allocator is used for free()". Nothing is dereferenced here. `size == 0` returns NULL *before* the call, so `malloc(0)`'s implementation-defined result never escapes, and every caller NULL-checks. The P4-120 injection hook returns NULL on the same path a real allocator would. | yes: via `tj3Alloc`, which is a **safe** `extern "C" fn` — allocating cannot violate anything, only the later free can. | `tj3_alloc_free_round_trips_through_malloc` (tests/alloc_and_errors.rs); `mem_dest_initial_allocation_failure_reports_oom_case_10` and `mem_dest_growth_failure_reports_oom_case_12` (tests/capi_alloc_failure_injection.rs) drive the NULL return through the injection hook, which is the only way any test reaches it. | Integration Tests, both oracle legs, through the whole C-ABI suite; ARMv7 via `capi_layout_adoption`'s `tj3LoadImage8` cases. Not ASan/UBSan and not ARMv7 `--lib` (no unit test in this crate allocates through libc), not Miri (Miri cannot run a foreign `malloc`), not the C-boundary harness (its decode path allocates nothing here). | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `libc_free` | 1 | Calls libc `free` on a pointer this crate handed out, for the same allocator-symmetry reason as `libc_malloc`. Rust has no safe way to release memory it does not own a `Box`/`Vec` for. | SAFETY comment: "pointer came from `malloc` (via `libc_malloc`)". NULL returns early. Nothing else is checkable — a raw pointer carries no provenance the callee can inspect — so the real obligation is the documented ownership contract on `tj3Free` and on `deliver_compressed_output`'s reallocating path. | yes: `libc_free` is a safe `pub(crate) fn`, and `tj3Alloc`'s counterpart `tj3Free` is `unsafe` precisely because this one cannot check anything. That asymmetry is deliberate (P4-137). | `tj3_alloc_free_round_trips_through_malloc` (tests/alloc_and_errors.rs); `tj3_compress8_keeps_the_callers_buffer` and `a_too_small_buffer_is_refused_rather_than_replaced` (tests/norealloc_all_entry_points.rs) prove the *reallocating* path frees the prior pointee and the NOREALLOC path does not. | Integration Tests, both oracle legs; ARMv7 via `capi_layout_adoption`. Not ASan/UBSan, not Miri, not the C-boundary harness. A double free here would be visible to ASan only if a test allocated and freed through the instrumented cdylib, which none does. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `libc_from_slice` | 1 | `memcpy` from a Rust slice into a fresh libc allocation. `copy_from_slice` would need a `&mut [u8]` over memory Rust does not own; the `memcpy` keeps the buffer squarely in the C allocator's world. | SAFETY comment: "`p` points to `data.len()` bytes of freshly allocated memory and does not alias the source slice". Both halves are established locally: `p` comes from `libc_malloc(data.len())` two lines above and is NULL-checked, and the destination is one call old, so it cannot alias `data`. | yes: safe `pub(crate) fn`, reached from `tj3GetICCProfile` and `deliver_compressed_output`, both behind `extern "C"` entry points. | `tj3_alloc_free_round_trips_through_malloc` (tests/alloc_and_errors.rs) and every reallocating compress case in `norealloc_contract_matches_upstream_turbojpeg` (tests/norealloc_all_entry_points.rs); the ICC path is exercised by `tj3_transform_buf_size_includes_icc_size` (tests/precision.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7 `--lib`, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `deliver_compressed_output` | 5 | `unsafe fn` declaration plus four blocks: reading `*jpeg_buf` and `*jpeg_size` (the caller's out-pair, and under `TJPARAM_NOREALLOC` an *input* capacity), `copy_nonoverlapping` into the caller's buffer, and writing both slots. One shared implementation for six compressing entry points — P4-145 was filed because the other five each had their own, and five of them freed the caller's slot unconditionally. | Documented `# Safety` section: both out-pointers non-NULL and valid for read and write; under NOREALLOC a non-NULL `*jpeg_buf` valid for `*jpeg_size` bytes and not aliasing `jpeg`; otherwise a non-NULL `*jpeg_buf` must have come from `malloc`/`tj3Alloc`. The one bound that *is* checked here rather than assumed is `jpeg.len() <= capacity`, tested before the copy. The reallocating arm frees `prior` only after `libc_from_slice` succeeded, so an OOM leaves the caller's slot intact. | no: C ABI — `pub(crate) unsafe fn`, called only by the six compressing entry points, each of which NULL-checks both pointers first. | `tj3_compress8_keeps_the_callers_buffer`, `tj3_compress12_keeps_the_callers_buffer`, `tj3_compress16_keeps_the_callers_buffer`, `tj3_compress_from_yuv8_keeps_the_callers_buffer`, `tj3_compress_from_yuv_planes8_keeps_the_callers_buffer`, `tj3_transform_keeps_the_callers_destination_buffers`, `a_too_small_buffer_is_refused_rather_than_replaced` and the C-oracle trace `norealloc_contract_matches_upstream_turbojpeg` (tests/norealloc_all_entry_points.rs); `tj3_compress8_norealloc_refuses_a_buffer_that_is_too_small` and `tj3_compress8_norealloc_writes_in_place_when_the_buffer_fits` (tests/norealloc_buffer_capacity.rs). | Integration Tests, both oracle legs, through those two suites; **ARMv7** via `capi_layout_adoption`'s `tj3_decompress12_writes_only_the_pitched_extent`, which reaches it through `tj3Compress12` before decoding. Not ASan/UBSan, not Miri, not the C-boundary harness (it decodes, never compresses). An overrun of the caller's NOREALLOC buffer would therefore be caught by the capacity assertion, not by a sanitizer. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3Free` | 1 | `pub unsafe extern "C" fn` declaration. The body only calls the safe `libc_free`; the `unsafe` is the *contract*, and that is the whole point of the row — a stack address, an interior pointer, one from another allocator or one already freed is undefined behaviour and nothing here can detect any of them (P4-137). | The caller's contract, unchecked: NULL, or a pointer from `tj3Alloc`/the same `malloc` that has not been freed. Detecting misuse needs generation-tagged handles in a registry, which P4-137 records as an open decision. The `unwind_guard!` wrapper only stops a panic crossing the boundary; it validates nothing. | no: C ABI. The `unsafe` marking exists so a *Rust* consumer of this crate's `rlib` cannot reach `free` without acknowledging it. | `tj3_alloc_free_round_trips_through_malloc` (tests/alloc_and_errors.rs); `tj3Free` is also the release path in almost every C-ABI suite, e.g. `tj3_compress8_via_dlopen_round_trips_64x64_rgb` (tests/compress8.rs), which calls it through the real cdylib. | Integration Tests, both oracle legs; also `symbol_inventory` on all three *CAPI ABI Checks* OSes, which proves the symbol is exported but never calls it; **ARMv7** via `capi_layout_adoption`, which releases both the `tj3LoadImage8` buffer and the `tj3Compress12` output through it. Not ASan/UBSan, not Miri, not the C-boundary harness (it frees with its own `free`, which is the point of the shared allocator and is *not* asserted anywhere). | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/bufsize.rs` — 2 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `tj3GetScalingFactors` | 2 | `pub unsafe extern "C" fn` declaration plus one write through the caller's optional `numScalingFactors` out-pointer. The returned array itself needs no `unsafe`: it is a `OnceLock<Vec<TjScalingFactor>>` handed out as `&'static [T]`, so the "valid for the duration of the process" lifetime the C API promises is a real Rust lifetime. | The write is guarded by an explicit `is_null()` test — this is one of the entry points whose out-pointer is genuinely optional, matching upstream. The `as_ptr() as *mut` cast is sound because the table is never mutated after `get_or_init`; the C signature is `tjscalingfactor *` for historical reasons only. | no: C ABI. | `tj3GetScalingFactors` is resolved and called through the cdylib by `symbol_inventory` (tests/symbol_inventory.rs); the factor list itself is pinned by `tj3_set_scaling_factor_halves_output` (tests/header_ops.rs) and by the root crate's `c_xval_scaling_all_factors_all_subsamplings` (tests/cross_check_scaling_factors.rs). **none** passes a non-NULL `numScalingFactors` and checks the written count. | *CAPI ABI Checks* on ubuntu, macos and windows (release) via `symbol_inventory`; Integration Tests, both oracle legs, through the same suite. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/compress.rs` — 5 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `tj3Compress8` | 5 | `pub unsafe extern "C" fn` declaration; `with_handle` on the caller's `tjhandle`; `slice::from_raw_parts` over the caller's pitched source; a second `from_raw_parts` per row on the repacking path; and `deliver_compressed_output` on the out-pair. A pitched C buffer has no safe Rust spelling — the length is the caller's assertion, not a fact the callee can derive. | The span is **checked, not assumed**: `checked_span([w, bpp])` for the row and `ImageLayout::strided(w, h, bpp, pitch)` for the whole buffer, which is the "every row but the last is a full pitch, the last needs only its own pixels" rule (`ImageLayout` owns it since P4-139; this site is the shape it was modelled on) and which proves the byte total fits `isize::MAX`, `from_raw_parts`' own precondition. Before P4-137 the same arithmetic used `checked_mul(h).unwrap_or(0)`, so an overflow produced a *zero-length* slice and the encode silently proceeded on no input. `src_buf`, `jpeg_buf`, `jpeg_size` are NULL-checked and `width`/`height`/`pitch` range-checked first; the body is a closure so `with_handle` — not the caller — picks the `&mut TjInstance` lifetime (P4-137). | no: C ABI. | `tj3_compress8_via_dlopen_round_trips_64x64_rgb` and `tj3_compress8_null_arguments_return_minus_one` (tests/compress8.rs); `tj3_compress8_refuses_a_source_span_past_isize_max` and the padded-pitch cases in tests/capi_layout_adoption.rs; `tj3_compress8_keeps_the_callers_buffer` (tests/norealloc_all_entry_points.rs); `c_client_compress8_smoke` (tests/compress8.rs) drives it from a compiled C client. | Integration Tests, both oracle legs. **Not ARMv7**: `capi_layout_adoption`'s only `tj3Compress8` case is `tj3_compress8_refuses_a_source_span_past_isize_max`, which sits in the `#[cfg(target_pointer_width = "64")]` `source_span_bounds` module and compiles out on the 32-bit leg — so this entry point's span arithmetic never runs at half pointer width. Not ASan/UBSan, not Miri, not the C-boundary harness (it decodes only). | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/decompress.rs` — 4 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `tj3Decompress8` | 4 | `pub unsafe extern "C" fn` declaration; `with_handle`; `slice::from_raw_parts` over the caller's JPEG; and `slice::from_raw_parts_mut` over the caller's destination, which the safe `repack_into_pitched` then writes through — so all the pixel arithmetic downstream of that one line is bounds-checked Rust. | `jpeg_buf`, `dst_buf` NULL-checked, `jpeg_size >= 2` and `pitch >= 0` checked, `effective_pitch >= w * dst_bpp` checked. The destination length is `effective_pitch.checked_mul(h)` — checked since P4-139, where `saturating_mul` had been yielding `usize::MAX` and claiming the whole address space. **Note the asymmetry with `tj3Compress8`:** the destination span is `pitch * height`, not `ImageLayout::strided`'s tighter `pitch * (h - 1) + row_bytes`. It matches what `turbojpeg.h:2077-2079` tells the caller to allocate ("This buffer should normally be `pitch * destinationHeight` samples in size"), and the writes stay inside the row extents, but the *slice* covers one row's trailing pad, so a caller who allocated only the tighter amount — which is all upstream's own `row_pointer` loop touches (`turbojpeg-mp.c:244-246`) — would have a reference formed over memory it does not own. Filed as P4-193. | no: C ABI. | `tj3_decompress8_round_trips_64x64_rgb` and `tj3_decompress8_null_arguments_return_minus_one` (tests/decompress8.rs); `regression_issue_369_gray_argb_abgr.rs` in the root crate pins the gray-to-ARGB/ABGR repack; `foreign_coef_arrays_full_round_trip_pixel_exact` (tests/capi_foreign_coef_arrays.rs). **none** allocates exactly `pitch * (h - 1) + row_bytes` and decodes into it, which is the case the note above describes. | Integration Tests, both oracle legs; **C-boundary ASan** — this is one of the five entry points the instrumented C harness drives, over baseline, progressive and arithmetic fixtures, so a write past the destination would trip ASan there. Not Miri, not ARMv7, not ASan/UBSan `--lib`. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/header.rs` — 7 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `tj3DecompressHeader` | 3 | `pub unsafe extern "C" fn` declaration; `slice::from_raw_parts` over the caller's JPEG; `with_handle` on the caller's handle. | `jpeg_buf` non-NULL and `jpeg_size >= 2` are checked before the slice is formed; the length itself is the caller's assertion and nothing can verify it. The handle-scoped body runs inside `with_handle`, so the `&mut TjInstance` cannot escape or alias a second one. | no: C ABI. | `tj3_decompress_header_populates_dimensions` (tests/header_ops.rs); `norealloc_contract_matches_upstream_turbojpeg` (tests/norealloc_all_entry_points.rs) calls it against the C oracle; `precision_fns_reject_null_and_unsupported_tjpf` (tests/precision.rs) covers the rejection arm. | Integration Tests, both oracle legs; **C-boundary ASan**, which calls it on every fixture before decoding. Not Miri, not ARMv7, not ASan/UBSan `--lib`. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3SetScalingFactor` | 2 | `pub unsafe extern "C" fn` declaration plus `with_handle`. `factor` is a `#[repr(C)]` struct passed **by value**, so nothing is dereferenced; the only raw pointer is the handle. | `num`/`denom` are rejected unless strictly positive, before `set_scaling_factor` sees them; the handle is NULL-checked by `with_handle`, which returns `None` and yields -1. | no: C ABI. | `tj3_set_scaling_factor_halves_output` (tests/header_ops.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3SetCroppingRegion` | 2 | `pub unsafe extern "C" fn` declaration plus `with_handle`. `region` is by value, like the scaling factor above. | `{0,0,0,0}` clears the region, per the C contract; otherwise `x`/`y` must be non-negative and `w`/`h` strictly positive before the region reaches `set_cropping_region`. Bounds against the image are the decoder's job, not this entry point's. | no: C ABI. | `tj3_set_cropping_region_validates_inputs` (tests/header_ops.rs); the geometry itself is cross-validated by the root crate's `c_croptest` suite. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/imageio.rs` — 24 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `cstr_to_path` | 2 | `unsafe fn` declaration plus `CStr::from_ptr` on the caller's `const char *` filename. Rust cannot know where a C string ends without walking it, which is exactly what `from_ptr` does under the caller's promise that a NUL is there. | NULL returns `None` before the walk. The rest is the caller's contract: NUL-terminated and valid for the whole scan. **The `&'static str` is a lie the SAFETY comment admits** — the real lifetime is the caller's storage, and it is cast to `'static` only to satisfy the borrow checker at the immediate use, which copies the bytes into an owned `Path`/`String` before this function's caller returns. Nothing enforces that discipline; it holds by inspection of the two call sites. | no: C ABI — private `unsafe fn`, called only by `tj3LoadImage8` and `tj3SaveImage8`. | `tj_load_image_reports_error_for_missing_file` (tests/legacy_aliases.rs) drives the non-NULL, valid-path arm; `png_round_trip_rgb` (tests/png_image_io.rs). **none** passes a non-UTF-8 filename, which is the `to_str().ok()` rejection arm. | Integration Tests, both oracle legs (the PNG cases need `--features png`, which the P4-175 step passes); ARMv7 via `capi_layout_adoption`. Not ASan/UBSan, not Miri, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3LoadImage8` | 8 | `pub unsafe extern "C" fn` declaration; `cstr_to_path`; a read of the caller's `*pixel_format` request; `copy_nonoverlapping` of each dense row into the libc-allocated, `align`-padded result; three writes to the optional `width`/`height`/`pixel_format` out-slots; and `with_handle`. The returned buffer must be libc-owned because the caller releases it with `tj3Free`. | Each of the four out-pointers is `is_null()`-tested before it is touched, which is the documented "optional" contract. `align` is validated as a positive power of two, as upstream requires, and the padded total comes from `checked_span` before `libc_malloc`, so the destination owns `total` bytes and `row_dense <= row_stride` keeps each copy inside its row. The source offsets come from an `ImageLayout` over the decoded image, not from caller arithmetic. | no: C ABI. | `tj3_load_image8_rejects_a_non_positive_align`, `tj3_load_image8_rejects_a_non_power_of_two_align`, `tj3_load_image8_pads_rows_to_a_power_of_two_align` (tests/capi_layout_adoption.rs); `png_round_trip_rgb`, `png_round_trip_rgba`, `png_round_trip_grayscale` (tests/png_image_io.rs); `tj_load_save_image_round_trip_ppm_through_legacy_alias` (tests/legacy_aliases.rs). | Integration Tests, both oracle legs; **ARMv7** via `capi_layout_adoption`, the one leg where the padded-row arithmetic runs at 32 bits with overflow checks on. Not ASan/UBSan, not Miri, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3LoadImage12` | 2 | `pub unsafe extern "C" fn` declaration plus `with_handle`. The body is a refusal: every other argument is ignored and NULL is returned with an error on the handle, because 12-bit image *file* loading is not routed through the Rust shim yet. | Nothing is dereferenced except the handle, and `with_handle` NULL-checks that. The declaration is `unsafe` for signature parity with its 8-bit sibling, not because the body needs it. | no: C ABI. | **none** — no suite calls `tj3LoadImage12`; `symbol_inventory` proves the symbol exists but does not invoke it. The refusal is unobserved. | *CAPI ABI Checks* (three OSes) confirms the export only. No leg executes the body. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3LoadImage16` | 2 | As `tj3LoadImage12`: declaration plus `with_handle`, body is the same refusal for 16-bit sources. | As above — only the handle is touched, and `with_handle` NULL-checks it. | no: C ABI. | **none** — same as `tj3LoadImage12`. | Export only, via *CAPI ABI Checks*. No leg executes the body. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3SaveImage8` | 5 | `pub unsafe extern "C" fn` declaration; `cstr_to_path`; `slice::from_raw_parts` over the caller's dense buffer; a per-row `from_raw_parts` on the pitched path; and `with_handle`. | `buffer` NULL-checked, `width`/`height` required positive and `pitch` required non-negative — the last one is P4-139's fix: `pitch <= 0` used to fold `-1` into "dense", so a caller got a file whose rows came from a stride it never asked for. The extent is `ImageLayout::strided(w, h, bpp, stride)`, so the last row reads only `row_bytes` and the byte total is proven under `isize::MAX`. The bottom-up flip reads the live `inst` borrow rather than re-dereferencing `handle`, which would alias two `&mut TjInstance`. | no: C ABI. | `tj3_save_image8_rejects_a_negative_pitch`, `tj3_save_image8_still_reads_pitch_zero_as_dense`, `tj3_save_image8_reads_a_padded_pitch_at_the_right_offsets` (tests/capi_layout_adoption.rs); `png_round_trip_rgb` and `png_save_feature_gate_error_no_feature` (tests/png_image_io.rs); `tj_load_save_image_round_trip_ppm_through_legacy_alias` (tests/legacy_aliases.rs). | Integration Tests, both oracle legs; **ARMv7** via `capi_layout_adoption`. Not ASan/UBSan, not Miri, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3SaveImage12` | 2 | `pub unsafe extern "C" fn` declaration plus `with_handle`; the body refuses, as the 12-bit loader does. | Only the handle is touched, NULL-checked by `with_handle`. | no: C ABI. | **none** — no suite calls it. | Export only, via *CAPI ABI Checks*. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3SaveImage16` | 3 | Declaration plus `with_handle`, as above — **and a third site that is not in the function at all**: the module-tail `const _IO_FREE_KEEPALIVE: unsafe fn(*mut u8) = libc_free;`, an `unsafe fn` *type* in a `const` that exists only to keep the `libc_free` import referenced. Attribution assigns a module-level site to the nearest preceding declaration, so it lands on this row; the count is right and the location is not, which is worth knowing before re-reading the row. | The two real sites touch only the handle. The keepalive constant dereferences nothing — it names a function pointer. | no: C ABI (the constant is `#[allow(dead_code)]` and unreachable). | **none** for the entry point — no suite calls `tj3SaveImage16`. The keepalive site has nothing to test. | Export only, via *CAPI ABI Checks*. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/legacy.rs` — 54 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `tjDestroy` | 2 | `pub unsafe extern "C" fn` declaration plus the forwarding call to `tj3Destroy`. Nothing is dereferenced here; the `unsafe` is the obligation being passed through unchanged. | Restated on this function and discharged unchanged: NULL or a live handle from `tjInit*`/`tj3Init`, never one already destroyed, and no concurrent call using it. Unverifiable here for exactly the reason `tj3Destroy` records. | no: C ABI. | `tj_init_destroy_compress_decompress_legacy_path` (tests/legacy_aliases.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. A double free through this alias is undetected everywhere. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjCompress2` | 8 | Declaration; `with_handle` for the NULL-`jpegSize` error report; three `tj3Set` forwards (quality, subsampling, NOREALLOC); a read of `*jpeg_size`; the `tj3Compress8` call; and the unconditional write back to `*jpeg_size`. Every one is an `unsafe fn` call across the shim's own C ABI. | `jpeg_size` is NULL-checked **first**, before any `tj3Set`, because the ordering is observable: setting `TJPARAM_NOREALLOC` and then failing would leave the handle's ownership behaviour changed by a call that returned -1, so the *next* call could free caller-owned storage. Under NOREALLOC the capacity handed down is recomputed as `tj3JPEGBufSize(width, height, subsamp)` rather than read from the slot, because the legacy slot is an **output only** — forwarding it directly turned a valid legacy call into "buffer too small". Both mirror `turbojpeg.c:1277-1287`. | no: C ABI. | `legacy_tj_compress2_honours_tjflag_norealloc`, `legacy_tj_compress2_treats_the_size_slot_as_an_output`, `legacy_invalid_arguments_do_not_change_ownership_state`, `legacy_null_size_pointer_is_rejected` (tests/norealloc_all_entry_points.rs, several traced against the C oracle); `tj_init_destroy_compress_decompress_legacy_path` (tests/legacy_aliases.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjDecompress2` | 2 | Declaration plus the single forwarding call to `tj3Decompress8`. The legacy `width`/`height` arguments are ignored, as documented — TJ3 reads them from the header. | Nothing is added here, so the invariant is entirely `tj3Decompress8`'s: `jpeg_buf` valid for `jpeg_size`, `dst_buf` valid for `pitch * height`. | no: C ABI. | `tj_init_destroy_compress_decompress_legacy_path` (tests/legacy_aliases.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjDecompressHeader3` | 4 | Declaration; the `tj3DecompressHeader` forward; `with_handle`; and one block writing the four optional out-slots (`width`, `height`, `jpegSubsamp`, `jpegColorspace`). | Each of the four is `is_null()`-tested inside the block — optional per the C contract — and the parse has already succeeded, so the values come from the handle rather than from the caller. | no: C ABI. | `tj_init_destroy_compress_decompress_legacy_path` (tests/legacy_aliases.rs) reads dimensions back through it. **none** passes a NULL for one of the four out-slots and a non-NULL for the others, which is the arm the `is_null()` tests exist for. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjTransform` | 13 | The crate's densest legacy bridge: declaration; four `with_handle` error reports; `slice::from_raw_parts` over the source JPEG; a second over the caller's `transforms` array; three more `with_handle` calls for the marker-registration probe and the two allocation-failure paths; the `tj3Transform` call on the capacity array; the per-slot `*dst_sizes.add(index)` write-back; and the direct `tj3Transform` call on the non-NOREALLOC path. It exists because upstream's legacy wrapper pre-reads the header to derive destination capacities (`turbojpeg.c:3127-3152`) and that pre-read has an observable side effect on marker saving (P4-156). | `n > 0` and the three array pointers non-NULL are checked before any handle state is touched, as in `tjCompress2` and for the same ownership reason. `jpeg_buf` non-NULL **and** `jpeg_size != 0` are checked before `from_raw_parts`, because that function requires a non-null pointer even for a zero-length slice — reaching it first turned a documented -1 into an abort in debug and UB in release. `transforms` is read as `n` entries on the caller's assertion. The COPYNONE quirk is applied to a *local copy*, never to the caller's array, and the copy is allocated with `try_reserve_exact` so a caller-controlled `n` cannot abort through Rust's infallible allocation path. | no: C ABI. | `legacy_tj_transform_maps_the_flag_with_a_nonzero_size_slot`, `legacy_tj_transform_fills_a_zero_dst_size_from_geometry`, `legacy_tj_transform_does_not_parse_the_source_into_the_handle`, `legacy_tj_transform_sizes_a_grayscale_source_as_gray`, `legacy_tj_transform_rejects_a_null_source_under_norealloc`, `legacy_invalid_arguments_do_not_change_ownership_state` (tests/norealloc_all_entry_points.rs). | Integration Tests, both oracle legs, several cases traced against real TurboJPEG. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjEncodeYUV3` | 3 | Declaration; the `tj3Set(TJPARAM_SUBSAMP)` forward; and the `tj3EncodeYUV8` call. `pitch` and `align` are forwarded raw. | `align` is deliberately **not** clamped here: forwarding it unchanged lets the TJ3 entry point apply upstream's own "positive power of two" check, and an earlier clamp masked the `align < 1` half of it — a silent accept where upstream refuses (#539 re-review). The source extent is `tj3EncodeYUV8`'s obligation. | no: C ABI. | `tj_encode_decode_yuv_legacy_aliases_roundtrip_444`, `tj_yuv_legacy_aliases_propagate_bottomup_flag`, `tj_yuv_legacy_aliases_actually_flip_rows_under_bottomup`, `tj_encode_yuv3_does_not_over_read_padded_input_buffer` (tests/legacy_aliases.rs); `legacy_encodeyuv3_align0_case` (tests/capi_compress_precision.rs) traces the `align == 0` refusal against the C oracle. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjDecodeYUV` | 3 | Declaration; the `tj3Set(TJPARAM_SUBSAMP)` forward; and the `tj3DecodeYUV8` call, with `align` forwarded raw for the same reason as `tjEncodeYUV3`. | As above: validation belongs to the TJ3 entry point, and the argument order is the upstream one (`align` third, `pitch` seventh) — the two are easy to transpose and the doc comment says so explicitly. | no: C ABI. | `tj_encode_decode_yuv_legacy_aliases_roundtrip_444` and `tj_yuv_legacy_aliases_actually_flip_rows_under_bottomup` (tests/legacy_aliases.rs); `legacy_decodeyuv_align0_case` (tests/capi_compress_precision.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `process_legacy_compress_flags` | 4 | A **safe** `fn` whose body is four calls to the `unsafe` `tj3Set`/`tj3Get`. It takes a raw `handle` and forwards it, so the obligation is the caller's — and both callers are `unsafe extern "C" fn`s that received the handle from C. | The handle's validity is `tjEncodeYUV3`'s / `tjCompress2`'s obligation, and each `tj3Set` NULL-checks through `with_handle` anyway. The `fastDCT` rule is upstream's, not `TJFLAG_FASTDCT`: `quality < 96 && !ACCURATEDCT`, with a fresh handle reading back -1 since P4-155 and `-1 < 96` selecting fast, exactly as `turbojpeg.c:554-558` does on its own unset default. **Being a safe `fn` taking a raw pointer is the P4-135 shape**; it is private to this module and reachable only from the two `extern "C"` wrappers, so no safe caller outside can name it. | no: private safe `fn`, but its argument is a raw pointer — see the Invariant cell. | `tj_yuv_legacy_aliases_propagate_bottomup_flag` and `tj_yuv_legacy_aliases_actually_flip_rows_under_bottomup` (tests/legacy_aliases.rs); the quality-driven `fastDCT` rule is pinned by `fresh_get_case` in tests/capi_compress_precision.rs. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `process_legacy_decompress_flags` | 3 | The decompress-side twin: a safe `fn` making three `unsafe` `tj3Set` calls. `fastDCT` is driven directly by `TJFLAG_FASTDCT` here, since there is no compression quality to balance against. | As the compress twin: the handle is the `extern "C"` caller's obligation and every `tj3Set` NULL-checks. | no: private safe `fn` taking a raw pointer, reachable only from `tjDecodeYUV`. | `tj_yuv_legacy_aliases_propagate_bottomup_flag` (tests/legacy_aliases.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `copy_handle_error_to_no_handle_slot` | 1 | A safe `fn` that forms `&*(handle as *const TjInstance)` to read the temporary handle's error message before it is destroyed, so `tjGetErrorStr2(NULL)` can report it — mirroring what upstream does for the handle-less legacy ABI. | NULL returns early. Beyond that the pointer is **not** the caller's: both call sites created it with `tj3Init` moments earlier and it never left the function, so the reference is provably live and unaliased. A shared `&` is used rather than `&mut`, so it cannot conflict with anything the handle-less wrappers do next. | no: private safe `fn`, called only by `tjLoadImage` and `tjSaveImage` on handles they own. | `tj_load_image_reports_error_for_missing_file` (tests/legacy_aliases.rs) is the test that reads the message back through the no-handle slot. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjLoadImage` | 4 | Declaration; the `tj3Set(TJPARAM_BOTTOMUP)` forward; the `tj3LoadImage8` call; and `tj3Destroy` on the temporary handle. The legacy 2.x signature is handle-less, so this allocates one, delegates, and frees it — as upstream does. | The temporary handle is created here, NULL-checked, never published, and destroyed on every exit path, so the `tj3Destroy` obligation is discharged locally rather than by the caller. `filename` and the three out-slots are `tj3LoadImage8`'s obligation. | no: C ABI. | `tj_load_image_reports_error_for_missing_file` and `tj_load_save_image_round_trip_ppm_through_legacy_alias` (tests/legacy_aliases.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjSaveImage` | 4 | The mirror of `tjLoadImage`: declaration, `tj3Set(TJPARAM_BOTTOMUP)`, the `tj3SaveImage8` call, and `tj3Destroy` on the temporary handle. | Same local-ownership argument for the temporary handle; `buffer` and `pitch` are `tj3SaveImage8`'s obligation, and it is the one that rejects a negative pitch. | no: C ABI. | `tj_load_save_image_round_trip_ppm_through_legacy_alias` (tests/legacy_aliases.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tjGetErrorStr2` | 2 | Declaration plus the forward to `tj3GetErrorStr`. The returned `*const c_char` points into the handle's own `CString`. | The handle is `tj3GetErrorStr`'s obligation, NULL falling back to the process-global no-handle slot. **The returned pointer is valid only until the next call on that handle**, which is upstream's contract too and which nothing here enforces — the next entry point takes a fresh `&mut TjInstance` and may rewrite the string. | no: C ABI. | `tj_load_image_reports_error_for_missing_file` (tests/legacy_aliases.rs) reads the NULL-handle fallback; `tj3_get_error_str_and_code_reflect_last_call` (tests/alloc_and_errors.rs) covers the handle path through the TJ3 spelling. | Integration Tests, both oracle legs; `symbol_inventory` on three OSes proves the export. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `identity` | 1 | `std::mem::zeroed()` to build an all-zero `TjTransform` in this file's `#[cfg(test)] mod tests`. Zeroing is not derivable safely because `TjTransform` holds a `*mut c_void` and an `Option<unsafe extern "C" fn ...>`. | SAFETY comment: `#[repr(C)]` plain data, all-zero is the identity transform. It is sound for this type specifically — a null `data` pointer and a `None` function pointer are both valid all-zero bit patterns — and would not be for a type with a niche-filled non-nullable field. | test-only: inside `#[cfg(test)] mod tests` in legacy.rs. | It is its own regression test: `single_component_source_is_sized_as_gray`, `three_component_source_keeps_the_probed_subsampling` and `each_transform_gets_its_own_transformed_geometry` (crates/libjpeg-turbo-rs-capi/src/legacy.rs) all build their fixtures with it. | **C-ABI unit tests** on the Integration Tests job (stable, uninstrumented); **ASan / UBSan** via `cargo test --workspace --lib`; **ARMv7** via `-p libjpeg-turbo-rs-capi --lib`. This is one of the few rows in this document a sanitizer actually executes. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/memmgr.rs` — 94 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `(module)` | 10 | Ten `pub type …Fn = unsafe extern "C" fn(...)` aliases — the eleven-slot `jpeg_memory_mgr` vtable's signatures (two allocators share `AllocFn`). They are function *types*, not calls: nothing here executes, but a vtable that libjpeg's own code invokes cannot be spelled with safe `fn` pointers, because the callee dereferences `cinfo` and its `mem` slot. | Each alias carries the contract in its doc comment: `cinfo` non-NULL with `mem` pointing at a `JpegMemoryMgr` from `create_memory_mgr`. The layout that makes the aliases load-bearing is pinned separately by the compile-time `offset_of!` assertion in `assert_memory_mgr_layout` and by the `abi_offsets` suite. | no: C ABI — these are the types stock `djpeg`/`cjpeg` call through `cinfo->mem->alloc_small` and friends. | The types themselves cannot break at runtime; the layout they describe is pinned by `abi_offsets` (tests/abi_offsets.rs) and exercised end to end by `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs). | *CAPI ABI Checks* on ubuntu, macos and windows (release) for the layout; Integration Tests and C Interop for the stock-tool link. Compile-time everywhere else. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `impl Send for Block` | 1 | `unsafe impl Send` for the owned-heap-block record: `Block` holds a `NonNull<u8>` and its `Layout`, and a raw pointer is not `Send` on its own, so `MemPool` would not be either. | SAFETY comment: "`Block` holds a raw pointer we own. Sending the pool across threads is fine because the C shim is single-threaded per cinfo (libjpeg's documented contract)." The block is never aliased — `push_block` is the only producer and `free_pool`/`Drop` the only consumers — so moving it moves sole ownership. The libjpeg contract, not this code, is what rules out two threads inside one `cinfo`. | no: `Block` is private to this module; nothing outside can hold one. | `mm1_create_destroy_no_leak_via_drop_counter` and `mm2_pool_isolation` (crates/libjpeg-turbo-rs-capi/src/memmgr.rs) exercise allocation and release, single-threaded. **none** moves a pool between threads, which is what the `Send` claim is about. | **C-ABI unit tests**, **ASan / UBSan** (`--workspace --lib`) and **ARMv7 `--lib`** via the `mm*` tests — but all single-threaded, so the claim itself is untested. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `push_block` | 1 | `std::alloc::alloc(layout)` — the pool hands out raw, uninitialised, 32-byte-aligned memory to C, which needs an allocation API that returns a pointer rather than an owning type. | The `alloc` precondition is a non-zero-size layout, established two lines above by the `size == 0` early return and by `Layout::from_size_align(size, 32)` returning `Err` otherwise. A NULL return is propagated as NULL rather than aborting. `pool_id` is bounds-checked against `JPOOL_NUMPOOLS` before indexing. `ALIGN_SIZE = 32` matches libjpeg-turbo's `WITH_SIMD` build, so an AVX2 load at the end of a row cannot straddle the allocation. `total_space_allocated` saturates on purpose: it is an accounting total feeding the budget check, and saturating only makes that check stricter. | no: private `fn`; every path in is a vtable callback that has already resolved `cinfo->mem`. | `mm1_create_destroy_no_leak_via_drop_counter`, `mm2_pool_isolation`, `mm3_sarray_read_write` (src/memmgr.rs); `budget_below_the_working_set_raises_no_backing_store` and `ample_budget_still_realizes` (tests/capi_max_memory_budget.rs) drive the accounting. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`** via the `mm*` tests — ASan sees the allocator here, which is the strongest coverage any row in this document has; plus Integration Tests through the whole classic surface, and the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `free_pool` | 1 | `std::alloc::dealloc(block.ptr, block.layout)` for each block in the pool. Rust's allocator needs the size and alignment back, which is why `Block` keeps the `Layout`. | SAFETY comment: "`block.ptr` came from `alloc(block.layout)` above; we haven't handed out ownership, so dealloc is the correct release path." Ownership is genuinely retained: the pointer handed to C is a copy, and the contract is that it dies with the pool. The virtual-array control `Box`es are cleared *before* the blocks they point into, so no control block outlives its buffer. `pool_id` is bounds-checked first. | no: private `fn`, reached from `free_pool_impl` and from `MemPool::drop`. | `mm2_pool_isolation` (src/memmgr.rs) frees the IMAGE pool and then reads a sentinel back out of a PERMANENT block, so a pool-index confusion is a use-after-free ASan would report. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests and the stock-tool link through `jpeg_destroy_*`. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `combined_from_mgr` | 1 | `unsafe fn` declaration for the cast recovering the enclosing `#[repr(C)] struct Combined { mgr, pool }` from the `*mut JpegMemoryMgr` C holds. The body is a pointer cast with no dereference; the `unsafe` is the offset assumption. | Documented: "`mgr` must be a pointer produced by `create_memory_mgr`, otherwise the offset math is undefined." `mgr` is the first field of `Combined`, so the cast is the identity — which is why it is a cast and not `offset_of!` arithmetic — and `#[repr(C)]` is what makes that guaranteed rather than incidental. | no: private `unsafe fn`. | Every `mm*` test in src/memmgr.rs reaches it through `pool_from_cinfo`; `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs) reaches it from real libjpeg consumers. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests, C Interop and the cross-arch legs via the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `pool_from_cinfo` | 5 | `unsafe fn` declaration plus four operations: `.add(1)` to reach the `mem` slot in the caller's `jpeg_common_struct`, the read of that slot, `combined_from_mgr`, and `&mut (*combined).pool`. There is no Rust type for "the second pointer-sized word of a struct libjpeg declared", which is what `jpeg_common_fields` puts `mem` at. | Documented: the caller guarantees `cinfo` points to a valid `jpeg_common_struct` layout. NULL `cinfo` and NULL `mem` both return `None` rather than aborting. The offset is the ABI's, pinned by `abi_offsets`. **The returned `&'a mut MemPool` takes its lifetime from the call site**, which is the P4-137 shape — but every caller is an `unsafe extern "C" fn` that drops it before returning, and libjpeg's one-thread-per-`cinfo` contract is what rules out a second live borrow. | no: private `unsafe fn`, called only from this module's vtable callbacks. | `mm1`..`mm4` (src/memmgr.rs) go through it on every call; `budget_below_the_working_set_raises_no_backing_store` (tests/capi_max_memory_budget.rs); `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs). | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests plus the stock-tool link. Not Miri — and Miri is the only tool that would see an aliasing violation here, so the `&mut` lifetime claim is argued rather than checked. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `alloc_small_impl` | 2 | `unsafe extern "C" fn` declaration plus the `pool_from_cinfo` call. This is the vtable slot `wrppm.c:331` and every other libjpeg consumer calls on its first line. | The declaration's contract is the vtable's: `cinfo` valid with `mem` from `create_memory_mgr`. `sizeofobject.max(1)` keeps `push_block`'s non-zero rule, and `align_up(.., 32)` reproduces libjpeg's own alignment rule so the next allocation stays aligned. A NULL pool yields a NULL return, which libjpeg treats as out of memory. | no: C ABI — reached through `cinfo->mem->alloc_small`. | `mm1_create_destroy_no_leak_via_drop_counter` and `mm2_pool_isolation` (src/memmgr.rs); `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs) drives it from stock `djpeg`. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests, C Interop and cross-arch via the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `alloc_large_impl` | 2 | Declaration plus `pool_from_cinfo`, identical in body to `alloc_small_impl` — libjpeg reserves `alloc_large` for requests past its pooling heuristic, and this port has no separate heuristic. | As `alloc_small_impl`. The two being identical is a deliberate simplification of `jmemmgr.c`, not an oversight: this port has no small-object arena to bypass. | no: C ABI. | `mm1_create_destroy_no_leak_via_drop_counter` (src/memmgr.rs) calls both slots; `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs). | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests plus the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `sarray_sample_size` | 4 | `unsafe fn` declaration; a read of `is_decompressor` at byte offset 32 of the caller's struct; and two reads of `data_precision` through the compress/decompress ABI mirrors. There is no safe way to ask "which of two C structs is this" — the answer is a byte at a fixed ABI offset. | Offset 32 for `is_decompressor` is the `jpeg_common_fields` layout, the same dispatch `jpeg_abort`/`jpeg_destroy` use, and is pinned by `abi_offsets`. NULL `cinfo` returns 1. **This function is a bug fix, not a convenience**: rows were sized at one byte per sample regardless of precision, and stock 12-bit consumers (`j12init_write_ppm` via wrppm's `alloc_sarray`) write two — a heap overflow the stock-tool link gate caught on `monkey12.jpg`. | no: C ABI — private `unsafe fn` called from `alloc_sarray_impl` and `realize_virt_arrays_impl`. | `sarray_rows_are_two_bytes_per_sample_at_12_bit`, `sarray_rows_are_two_bytes_per_sample_at_16_bit`, `sarray_rows_stay_compact_at_8_bit` (tests/capi_alloc_sarray_precision.rs); `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs) is where the original overflow surfaced. | Integration Tests, both oracle legs, via `capi_alloc_sarray_precision`; C Interop and cross-arch via the stock-tool link. **C-ABI unit tests**, **ASan / UBSan** and **ARMv7 `--lib`** do execute it, through `alloc_sarray_impl` in the `mm3`/`mm4` tests — but `CinfoMock` is all-zero, so those legs only ever take the `data_precision <= 8` arm and the two-bytes-per-sample rule is pinned by the integration suite alone. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `alloc_sarray_impl` | 4 | Declaration; `sarray_sample_size`; `pool_from_cinfo`; and the loop writing each row pointer with `*ptr_array.add(r) = data_raw.add(r * row_bytes)`. The output is a `JSAMPARRAY` — an array of raw row pointers — which is the shape libjpeg's callers index. | SAFETY comment: "`data_raw` owns `row_bytes * rows` contiguous bytes; each stride is a valid offset within that block." Both allocations are NULL-checked before use, and zero `samplesperrow`/`numrows` return NULL early. Rows are padded to `2 * ALIGN_SIZE = 64` bytes so a SIMD upsampler can overwrite past a row's end without touching the next one. **`row_bytes * rows` and `rows * size_of::<JSampRow>()` are unchecked products** on `JDimension` (`u32`) inputs; on a 64-bit target `u32 * u32 * 2` cannot overflow `usize`, but on a 32-bit one it can, and the guard is `push_block`'s `Layout::from_size_align` returning `Err` on an absurd size rather than an explicit check here. | no: C ABI — reached through `cinfo->mem->alloc_sarray`. | `mm3_sarray_read_write` (src/memmgr.rs) writes and reads back every byte of a 100x200 array; `sarray_rows_are_two_bytes_per_sample_at_12_bit` (tests/capi_alloc_sarray_precision.rs); `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs). | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`** via `mm3` — ARMv7 is also the leg where the 32-bit product would show; Integration Tests and the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `alloc_barray_impl` | 3 | Declaration; `pool_from_cinfo`; and the row-pointer write loop, as `alloc_sarray_impl` but with `JBLOCK` (`JCOEF[64]`) rows. No precision dispatch: a coefficient is always two bytes. | Same SAFETY argument and the same NULL checks. `row_bytes = blocksperrow * 128` and the pointer-array size are unchecked products over `u32` inputs, bounded on 64-bit by construction and by `Layout::from_size_align` elsewhere. | no: C ABI. | `mm4_virt_barray_roundtrip` (src/memmgr.rs) reaches it through `realize_virt_arrays`; `foreign_coef_arrays_full_round_trip_pixel_exact` (tests/capi_foreign_coef_arrays.rs) round-trips real coefficient arrays through it; `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs) drives it from `jpegtran`. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`** via `mm4`; Integration Tests and the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `request_virt_sarray_impl` | 2 | Declaration plus `pool_from_cinfo`. The control block is a `Box<JVirtSarrayControl>` whose raw pointer is handed to C and whose ownership stays in `pool.virt_sarrays` — `Vec<Box<T>>` rather than `Vec<T>` precisely so a later `push` cannot reallocate the storage under a control pointer already in flight. | The returned pointer is valid until `free_pool(JPOOL_IMAGE)` or `self_destruct`, which is the documented lifetime and is enforced by where the `Box` lives. No allocation happens yet; `realize_virt_arrays` does that. | no: C ABI. | `mm4_virt_sarray_roundtrip` (src/memmgr.rs); `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs) drives it from stock `jpegtran`'s multi-scan path. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests and the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `request_virt_barray_impl` | 2 | Declaration plus `pool_from_cinfo`, the coefficient-array twin of `request_virt_sarray_impl` with the same `Vec<Box<T>>` pointer-stability argument. | As above. | no: C ABI. | `mm4_virt_barray_roundtrip` (src/memmgr.rs); `foreign_coef_arrays_full_round_trip_pixel_exact` (tests/capi_foreign_coef_arrays.rs). | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests and the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `realize_virt_arrays_impl` | 17 | The densest item in this document: declaration; the budget pre-pass reading `max_memory_to_use` and `total_space_allocated` through the `mem` slot; `pool_from_cinfo` five times; `sarray_sample_size` twice; the `alloc_sarray_impl`/`alloc_barray_impl` calls; two `write_bytes` pre-zero loops through the row pointers; and four writes to the control blocks through `ctrl_raw`. The raw control pointer exists because the loop cannot hold a `&mut MemPool` across the `alloc_*` call that reborrows it. | SAFETY comments on the pre-zero loops ("`buffer` has `numrows` rows; each row points to `row_bytes` contiguous bytes") and on the control writes ("`ctrl_raw` originated from a live `Box` in the pool; no one else holds a concurrent reference") — the latter holds because the snapshot block ends before `ctrl_raw` is used. `row_bytes` in the pre-zero loop is recomputed with the same `align_up(.., 64)` and the same `sarray_sample_size` the allocation used, so the two cannot disagree. The budget is `virt_array_maximum_space` with `checked_mul`/`checked_add` throughout (upstream's `SIZE_MAX - maximum_space < new_space` written as Rust), and a geometry that cannot be expressed raises `JERR_OUT_OF_MEMORY` with upstream's case number rather than borrowing the budget's `JERR_NO_BACKING_STORE`. Already-realized controls are skipped, matching upstream's `mem_buffer == NULL` filter, so a second call is a no-op rather than a double charge. | no: C ABI. | `mm4_virt_sarray_roundtrip` and `mm4_virt_barray_roundtrip` (src/memmgr.rs); `budget_below_the_working_set_raises_no_backing_store`, `ample_budget_still_realizes`, `non_positive_budget_means_unlimited` (tests/capi_max_memory_budget.rs); `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs). | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`** via the two `mm4` tests — the pre-zero loops write through every row pointer, so an off-by-one row stride is an ASan heap-buffer-overflow there; Integration Tests via `capi_max_memory_budget`; the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `access_virt_sarray_impl` | 3 | Declaration; `&mut *ctrl` on the control block C holds; and `mem_buffer.add(start_row)` to return the requested window. libjpeg's contract is "give me rows `[start, start+num)` of that virtual array", which is pointer arithmetic by construction. | SAFETY comments on both operations. NULL `ctrl` and an unrealized `mem_buffer` both return NULL. The window is bounds-checked with `start_row.saturating_add(num_rows) > rows_in_array`, which mirrors upstream's `JERR_BAD_VIRTUAL_ACCESS` contract except that this port returns NULL instead of calling `error_exit` — it has no error-manager handle here. `saturating_add` is right for a bound test: a wrap would have to compare *smaller*, and saturating makes it compare larger. | no: C ABI. | `mm4_virt_sarray_roundtrip` (src/memmgr.rs) walks every 4-row window of a 100-row array and writes a sentinel into each. **none** requests a window past `rows_in_array` and asserts NULL, so the bound test itself is unexercised. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`** via `mm4`; the stock-tool link. Not Miri, not Integration Tests directly. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `access_virt_barray_impl` | 3 | Declaration; `&mut *ctrl`; and `mem_buffer.add(start_row)` — the coefficient-array twin, and the slot `jpegtran` and `jpeg_read_coefficients` call. | Identical to `access_virt_sarray_impl`, including the `saturating_add` bound and the NULL-rather-than-`error_exit` divergence. | no: C ABI. | `mm4_virt_barray_roundtrip` (src/memmgr.rs); `foreign_coef_arrays_full_round_trip_pixel_exact` and `finish_decompress_clears_coef_cache_for_reuse` (tests/capi_foreign_coef_arrays.rs). **none** requests an out-of-range window here either. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests via `capi_foreign_coef_arrays`; the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `free_pool_impl` | 2 | Declaration plus `pool_from_cinfo`; the release itself is the safe `MemPool::free_pool`. | The vtable contract, plus `free_pool`'s own pool-index bound. A NULL pool is a silent no-op, which is what libjpeg expects of a manager whose `cinfo` is already torn down. | no: C ABI. | `mm2_pool_isolation` (src/memmgr.rs) calls the slot directly and then proves the other pool survived. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; the stock-tool link through `jpeg_finish_decompress`. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `self_destruct_impl` | 4 | Declaration; `.add(1)` to the `mem` slot; the read of it; and the block that NULLs the slot and calls `destroy_memory_mgr`. | SAFETY comment: the slot is cleared **before** the manager is freed, so a reentrant `self_destruct` — which libjpeg's `jpeg_destroy` can produce if a consumer calls it twice — reads NULL and returns instead of double-freeing. NULL `cinfo` and NULL `mgr` both return early. After this returns `cinfo->mem` is already NULL, which is stronger than the doc comment's "the caller must set `mem = NULL` immediately". | no: C ABI. | `mm1_create_destroy_no_leak_via_drop_counter` (src/memmgr.rs) reaches it through `with_cinfo`'s teardown, which every `mm*` test uses; `stock_tools_link_against_our_shim` (root tests/capi_stock_tool_link.rs) exercises it from every stock tool's exit path. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`** via every `mm*` test — a double free here is exactly what ASan reports; the stock-tool link. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `destroy_memory_mgr` | 3 | `pub unsafe fn` declaration; `combined_from_mgr`; and `Box::from_raw` reclaiming the allocation `create_memory_mgr` surrendered with `Box::into_raw`. Rust cannot express "take this pointer back into a `Box`" safely. | Documented: "`mgr` must be NULL or a pointer returned by `create_memory_mgr` that has not already been freed." NULL returns early. Dropping the `Box` runs `MemPool::drop`, which frees the IMAGE and PERMANENT pools in that order — so no control block outlives its buffer. Nothing detects a second call; `self_destruct_impl` prevents the one path that could produce it by NULLing the slot first. | no: `pub unsafe fn`, crate-internal — called by `self_destruct_impl` and by `jpeglib.rs`'s destroy paths. | `mm1_create_destroy_no_leak_via_drop_counter` (src/memmgr.rs) and every other `mm*` test via `with_cinfo`. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**; Integration Tests and the stock-tool link through `jpeg_destroy_*`. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `with_cinfo` | 1 | Test scaffold: one block that reads `cinfo.mem`, unwraps the `self_destruct` slot and calls it on a `CinfoMock` cast to `*mut c_void`. The mock exists because a real `jpeg_decompress_struct` is not needed — the vtable only reads the second pointer-sized word. | SAFETY comment: "we produced `mem` via `create_memory_mgr` and no vtable call can outlive this scope." `CinfoMock` is `#[repr(C)]` with `err` then `mem`, so the offset the manager reads is the ABI's. | test-only: inside `#[cfg(test)] mod tests` in src/memmgr.rs. | It is the harness for `mm1`..`mm4`; a mistake here fails all five of them. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**. Not Miri, not Integration Tests. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `mm1_create_destroy_no_leak_via_drop_counter` | 3 | Test body: `&mut *cinfo.mem` to reach the vtable, then two calls through the `alloc_small`/`alloc_large` slots. Calling an `unsafe extern "C" fn` pointer is the only way to test the vtable as C sees it. | The manager was created by `with_cinfo` moments earlier and the `CinfoMock` outlives the calls. The test's own claim — no allocation outlives the manager — is checked by ASan and by `MemPool::drop`, not by the drop counter, which only tallies the two calls. | test-only. | It is its own regression test. | **C-ABI unit tests**, **ASan / UBSan** (this is where the leak claim is actually checked), **ARMv7 `--lib`**. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `mm2_pool_isolation` | 4 | Test body: the vtable reborrow, 200 `alloc_small` calls across both pools, a sentinel write through a raw `*mut u8`, and a block that frees the IMAGE pool and reads the PERMANENT sentinel back. | The sentinel pointer is one of the 100 PERMANENT allocations, which `free_pool(JPOOL_IMAGE)` must not touch — reading it back after the free is the assertion, and under ASan a pool-index confusion is a use-after-free rather than a wrong byte. | test-only. | It is its own regression test. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `mm3_sarray_read_write` | 4 | Test body: the vtable reborrow, one `alloc_sarray` call, and two loops writing and reading every byte of a 100x200 array through `*arr.add(r)` row pointers. | Each row pointer is the one `alloc_sarray_impl` wrote, and the loop bound is the `cols` it was asked for, which is `<= row_bytes` after the 64-byte round-up. A row-stride error is an out-of-bounds write ASan reports. | test-only. | It is its own regression test, and the only one that writes every byte of an `alloc_sarray` result. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `mm4_virt_sarray_roundtrip` | 7 | Test body: the vtable reborrow, `request_virt_sarray`, `realize_virt_arrays`, a windowed `access_virt_sarray` per 4-row step with a sentinel write through `*window`, a final full-height access, and the read-back. Seven blocks because each vtable call is its own. | Each window is `[start, start+4)` of a 100-row array with `maxaccess = 4`, so every access is inside the bound `access_virt_sarray_impl` checks; the sentinel is written to the window's first row, which is `mem_buffer[start]`. | test-only. | It is its own regression test, and the only exercise of the virtual-sarray lifecycle end to end. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `mm4_virt_barray_roundtrip` | 5 | Test body: the vtable reborrow, `request_virt_barray`, `realize_virt_arrays`, one `access_virt_barray` window, and a write-then-read of coefficient 0 through `*window` as a `*mut JBlock`. | A 40-row array accessed at `[0, 4)` with `maxaccess = 4`, inside the bound. Writing `(*blk_row)[0]` indexes a `[i16; 64]` through a raw pointer to a block the allocator just produced. | test-only. | It is its own regression test. | **C-ABI unit tests**, **ASan / UBSan**, **ARMv7 `--lib`**. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/message_table.rs` — 1 site

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `impl Sync for MessageTable` | 1 | `[*const u8; N]` is not `Sync`, so the 129-entry `jpeg_message_table` a `jpeg_error_mgr` points at cannot be a plain `static` without this. The table must be raw pointers because C reads it as `const char * const []`. | SAFETY comment: "every pointer targets a `'static` NUL-terminated byte string that is never mutated, so sharing the table across threads cannot race." Both halves are structural: the strings are `b"…\0"` literals in this file, and `build_table` is a `const fn` that only copies their `as_ptr()`, so the `static` is initialised at compile time and has no writer at all. | yes: via `message_table_ptr()`, a safe `pub(crate) fn` reached from `jpeg_std_error` and installed in every caller's `jpeg_error_mgr`. | `format_message` (crates/libjpeg-turbo-rs-capi/tests/format_message.rs) renders messages through the installed table on all three OSes; `capi_error_message_rendering.rs` and `capi_classic_error_codes.rs` compare the rendered text against the C oracle. | **Windows**, which runs `format_message` by name — one of only two suites that leg executes; Integration Tests, both oracle legs, via all three suites. Not ASan/UBSan, not Miri, not ARMv7. No leg reads the table from two threads, which is what the `Sync` claim is about; the claim is structural rather than tested. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/mozjpeg_compat.rs` — 9 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `jpeg_c_bool_param_supported` | 1 | `pub unsafe extern "C" fn` declaration; the body returns `JPEG_FALSE` and touches nothing. These nine symbols exist so a consumer linked against mozjpeg (Homebrew libvips, Pillow-mozjpeg, several distros) can dyld-resolve against this cdylib — an undefined reference fails at load time even when the consumer never calls it (P2-10). | Nothing to establish: `_cinfo` is ignored and never dereferenced. The declaration is `unsafe` for signature parity with the rest of the exported surface, not because the body needs it. | no: C ABI. | **none** calls it. `capi_libvips_compat.rs` is the reason these exist and proves the *load-time* resolution when Homebrew `vips` is present and mozjpeg-linked; it skips otherwise, and it never invokes the stub. | Integration Tests, both oracle legs, run the libvips smoke when `vips` is installed. Nothing executes the body. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `jpeg_c_set_bool_param` | 1 | Declaration; the body is an empty `unwind_guard!` block. A well-behaved consumer probes `*_supported` first, but one that ignores the probe must not crash. | Nothing is dereferenced. | no: C ABI. | **none** — as above. | As above: exported and load-time-resolvable, never executed. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `jpeg_c_get_bool_param` | 1 | Declaration; returns `JPEG_FALSE`. | Nothing is dereferenced. | no: C ABI. | **none** — as above. | As above. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `jpeg_c_int_param_supported` | 1 | Declaration; returns `JPEG_FALSE`. | Nothing is dereferenced. | no: C ABI. | **none** — as above. | As above. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `jpeg_c_set_int_param` | 1 | Declaration; no-op body. | Nothing is dereferenced. | no: C ABI. | **none** — as above. | As above. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `jpeg_c_get_int_param` | 1 | Declaration; returns zero. | Nothing is dereferenced. | no: C ABI. | **none** — as above. | As above. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `jpeg_c_float_param_supported` | 1 | Declaration; returns `JPEG_FALSE`. | Nothing is dereferenced. | no: C ABI. | **none** — as above. | As above. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `jpeg_c_set_float_param` | 1 | Declaration; no-op body. | Nothing is dereferenced. | no: C ABI. | **none** — as above. | As above. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `jpeg_c_get_float_param` | 1 | Declaration; returns `0.0`. | Nothing is dereferenced. | no: C ABI. | **none** — as above. | As above. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/precision.rs` — 17 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `tj3Compress12` | 4 | `pub unsafe extern "C" fn` declaration; `slice::from_raw_parts` over the caller's `int16_t` sample buffer; `deliver_compressed_output` on the out-pair; and `with_handle`. | The span comes from `SampleGrid`, which wraps an `ImageLayout` whose element is one **sample**: `turbojpeg.h` measures `pitch` in samples while `from_raw_parts`' precondition is about *bytes*, so the ×2 has to be in the chain. It was not before P4-139 — a bare `pitch.checked_mul(height)` went straight to `from_raw_parts` over `i16`, so a geometry whose sample count fits `usize` but whose byte span does not built an out-of-contract slice. `SampleGrid::new` also separates "width × components alone is unrepresentable" from "pitch × height overflowed", because on a 32-bit target the first fires before pitch is even involved. | no: C ABI. | `tj3_compress12_decompress12_round_trips_gray_ramp` and `tj3_compress12_lossless_precision10_writes_sof_byte_10` (tests/precision.rs); `tj3_compress12_refuses_a_source_span_past_isize_max` (tests/capi_layout_adoption.rs); `tj3_compress12_keeps_the_callers_buffer` (tests/norealloc_all_entry_points.rs). | Integration Tests, both oracle legs; **ARMv7** via `capi_layout_adoption`, which is where a 32-bit sample-count wrap would surface. Not ASan/UBSan, not Miri, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3Decompress12` | 4 | Declaration; `from_raw_parts` over the caller's JPEG; `from_raw_parts_mut` over the caller's `int16_t` destination; and `with_handle`. The row copies themselves are safe slice operations on that one `&mut [i16]`. | `jpeg_buf`, `dst_buf` non-NULL, `jpeg_size >= 2` and `pitch >= 0` are checked first; the destination span is a `SampleGrid` built from the *decoded* image's dimensions and the caller's pitch, so the pitch-too-small case is an error rather than an overlapping write. The component count implied by the requested `TJPF` is compared against the decoded frame before anything is written. | no: C ABI. | `tj3_compress12_decompress12_round_trips_gray_ramp` and `precision_fns_reject_null_and_unsupported_tjpf` (tests/precision.rs); `tj3_decompress12_writes_only_the_pitched_extent` (tests/capi_layout_adoption.rs). | Integration Tests, both oracle legs; **ARMv7** via `capi_layout_adoption`. Not ASan/UBSan, not Miri, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3Compress16` | 5 | Declaration; a read of `*jpeg_buf` and `*jpeg_size` for the NOREALLOC precedence check; `from_raw_parts` over the caller's `uint16_t` samples; `deliver_compressed_output`; and `with_handle`. The extra site is the precedence check — one site more than its 12-bit sibling. | Both out-pointers are NULL-checked before the slot is read. The precedence itself is measured, not guessed: against TurboJPEG 3, a lossy 16-bit call with a NULL slot reports the buffer error, while the same call with a too-small but non-zero capacity reports the precision error — so only the NULL/zero case needs testing before the precision gate. The span is `SampleGrid`'s, as for 12-bit. The precision gate is the lossless flag, not `TJPARAM_PRECISION`, because upstream reads that parameter only when the flag is set. | no: C ABI. | `tj3_compress16_decompress16_is_lossless` and `tj3_compress16_lossless_precision14_writes_sof_byte_14` (tests/precision.rs); `lossy_16bit_compress_is_refused`, `requesting_12bit_precision_does_not_rescue_a_lossy_16bit_compress` and the C-oracle trace `precision_rules_match_upstream_turbojpeg` (tests/capi_compress_precision.rs); `tj3_compress16_refuses_a_source_span_past_isize_max` (tests/capi_layout_adoption.rs). | Integration Tests, both oracle legs. Not ARMv7 — `capi_layout_adoption`'s `tj3Compress16` case is in the `cfg(target_pointer_width = "64")` `source_span_bounds` module and compiles out there. Not ASan/UBSan, not Miri, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3Decompress16` | 4 | Declaration; the JPEG slice; the `uint16_t` destination slice; and `with_handle` — the lossless 16-bit twin of `tj3Decompress12`. | Identical to `tj3Decompress12`: the same argument checks, the same `SampleGrid` destination span, the same component-count comparison against the decoded frame. | no: C ABI. | `tj3_compress16_decompress16_is_lossless` and `precision_fns_reject_null_and_unsupported_tjpf` (tests/precision.rs). **none** exercises a padded pitch on this entry point specifically — `capi_layout_adoption` pins the 12-bit destination and the 16-bit *source*. | Integration Tests, both oracle legs. Not ARMv7 (no named suite reaches it), not ASan/UBSan, not Miri, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/tj3.rs` — 18 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `with_handle` | 2 | `unsafe fn` declaration plus `&mut *(handle as *mut TjInstance)`. This is the crate's single point of handle dereference, and it exists in this shape because of P4-137: the previous `handle_as_mut<'a>(*mut c_void) -> Option<&'a mut TjInstance>` let the **caller** choose `'a`, so two calls could produce two live `&mut` to one instance with nothing in the type system to flag it. | Documented in four bullets: `handle` NULL or from `tj3Init` and not destroyed; correctly aligned and live; **exclusivity** — no other `with_handle` on the same handle active on any thread, including through a C callback; and `f` must not destroy the handle. NULL is checked here. The rest is unverifiable — a raw pointer carries no provenance — but the closure shape means `'a` is chosen by `with_handle`, so a second overlapping `&mut` is no longer constructible by ordinary code. Generation-tagged handles in a registry is what would close the remainder; P4-137 records that as an open decision. | no: `pub(crate) unsafe fn`; every caller is an `extern "C"` entry point. | `handle_as_mut_is_gone_from_every_module`, `with_handle_confines_the_borrow_to_a_closure`, `set_and_get_still_round_trip_through_with_handle`, `null_handle_still_returns_the_error_sentinel` (tests/handle_borrow_scope.rs) — the first is a source-level grep gate, so the old signature cannot come back. | Integration Tests, both oracle legs; **C-boundary ASan** reaches it through all five entry points the harness drives; **ARMv7** through every `capi_layout_adoption` entry point. Not Miri — which is the only tool that would see an aliasing violation, so the exclusivity claim is argued, not checked. Not ASan/UBSan `--lib`. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3Destroy` | 2 | Declaration plus `Box::from_raw(handle as *mut TjInstance)`. Ownership transfers here; `Box::into_raw` in `tj3InitVersion` is the other half. | Documented: NULL or a live handle from `tj3Init`, not already destroyed, and no other thread inside a call using it. **Calling it twice is a double free and no check here can catch it** — the pointer looks identical both times. NULL returns early. `tj3InitVersion` is the only producer, and it is a *safe* `extern "C" fn`, which is correct: allocating breaks nothing. | no: C ABI. | `tj3_handle_lifecycle_via_dlopen` (tests/tj3_handle_dlopen.rs) creates and destroys through the real cdylib; every C-ABI suite destroys its handles, so a leak or a mismatched free would surface broadly. **none** asserts on a double destroy, which is the case the doc calls out. | Integration Tests, both oracle legs; **C-boundary ASan**, which destroys each handle under ASan — the one leg that would report a double free if a test made one; **ARMv7** via `capi_layout_adoption`, which destroys every handle it creates. Not Miri. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3Set` | 2 | Declaration plus `with_handle`. The parameter and value are plain `c_int`s; nothing else is dereferenced. | `param` is mapped through `param_from_c`, which rejects unknown ids, and `is_read_only` rejects `Width`/`Height`; the range check itself is the Rust `TjInstance::set`'s. `TJPARAM_PRECISION` is deliberately writable, because encode callers set it before a lossless compress. | no: C ABI. | `set_and_get_still_round_trip_through_with_handle` (tests/handle_borrow_scope.rs); `tj3_set_rejects_globally_invalid_precision` (tests/precision.rs); used by nearly every C-ABI suite. | Integration Tests, both oracle legs; **ARMv7** via `capi_layout_adoption`'s `tj3_decompress12_writes_only_the_pitched_extent`. Not Miri, not ASan/UBSan, not the C-boundary harness (it reads with `tj3Get` but never sets). | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3Get` | 2 | Declaration plus `with_handle`. | `param_from_c` rejects unknown ids with -1 and an error on the handle; a NULL handle yields -1 through `with_handle`'s `None`. | no: C ABI. | `set_and_get_still_round_trip_through_with_handle` and `null_handle_still_returns_the_error_sentinel` (tests/handle_borrow_scope.rs); `tj3_decompress_header_populates_dimensions` (tests/header_ops.rs) reads the parsed geometry back through it. | Integration Tests, both oracle legs; **C-boundary ASan**, which calls it to size its destination buffer. Not Miri, not ARMv7, not ASan/UBSan `--lib`. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3GetErrorStr` | 2 | Declaration plus `&mut *(handle as *mut TjInstance)` — this one does **not** go through `with_handle`, because it returns a pointer *into* the instance and the closure form would end the borrow before the pointer escapes. | NULL falls back to the process-global no-handle slot and then to a `'static` "No error", so a NULL handle is always answerable — which `tjunittest`'s `bufSizeTest()` relies on. **The returned `*const c_char` is valid only until the next call on that handle**: it is derived from a `&mut` that ends at the return, and the next entry point takes a fresh one. That is upstream's contract too, and nothing here enforces it; under Miri's Stacked Borrows, reading the string after a later `tj3Set` on the same handle would be a use of a tag the new borrow invalidated. | no: C ABI. | `tj3_get_error_str_and_code_reflect_last_call` (tests/alloc_and_errors.rs); `null_handle_still_returns_the_error_sentinel` (tests/handle_borrow_scope.rs); read by most negative-path assertions in the C-ABI suites. | Integration Tests, both oracle legs; **ARMv7** via `capi_layout_adoption`'s `error_string` helper, which every case there calls. **Not Miri** — and Miri is precisely the tool that would see the stale-pointer pattern described above, so no leg tests it. Not ASan/UBSan, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3GetErrorCode` | 2 | Declaration plus the same direct `&mut *` dereference, for symmetry with `tj3GetErrorStr` rather than necessity — it returns a `c_int` by value and would work through `with_handle`. | NULL returns `TJERR_FATAL`, as upstream does. The `&mut` is immediately dropped, so no pointer escapes; a shared `&` would be enough and is not what the code does. | no: C ABI. | `tj3_get_error_str_and_code_reflect_last_call` (tests/alloc_and_errors.rs); `tj3GetICCProfile`'s warning-versus-fatal distinction is read through it in `a5_a6_worker.rs` (root crate). | Integration Tests, both oracle legs. Not Miri, not ARMv7, not ASan/UBSan, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3GetICCProfile` | 3 | Declaration; one block covering both out-slot writes and the `libc_from_slice` allocation; and `with_handle`. `*iccBuf` receives a libc allocation the caller frees with `tj3Free`, so it cannot be a `Vec`. | `icc_size` is required and NULL-checked first; `icc_buf` is optional and `is_null()`-tested at each use, which is the query-only mode. On the no-profile path `*icc_size = 0`, `*icc_buf = NULL`, a **warning** (not fatal) is recorded and -1 returned — matching upstream, so a caller that inspects `tj3GetErrorCode` can tell "no ICC" from a real failure. An OOM from `libc_from_slice` returns -1 with `*icc_size` already written, which is upstream's behaviour too. | no: C ABI. | `a5_a6_worker.rs` (root crate) drives the profile round trip and the warning code. **none** in the C-ABI crate's own suites calls it. | Integration Tests and C Interop, through the root crate's `cargo test --tests`, plus the cross-arch legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3SetICCProfile` | 3 | Declaration; `slice::from_raw_parts(icc_buf, icc_size)` over the caller's profile; and `with_handle`. The bytes are copied into an owned `Vec` immediately, so the borrow does not outlive the call. | A null `icc_buf` **or** a zero `icc_size` clears the stored profile and skips the slice entirely — the NULL branch is upstream's semantics, and the earlier "NULL with size > 0 is an error" reading meant a caller could not remove a profile it had set (found in the codex review of `d4c28b1`). The length is the caller's assertion. | no: C ABI. | `tj3_transform_buf_size_includes_icc_size` (tests/precision.rs) sets a profile and reads its effect on the bound. **none** covers the clearing branch — setting a profile and then passing NULL to remove it, which is the case the fix above was made for. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/transform.rs` — 9 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `(module)` | 1 | The `Option<unsafe extern "C" fn(...) -> c_int>` field type of `TjTransform::custom_filter` — the coefficient-filter callback `tjtransform` declares. A function *type* at module level, not a call: attribution keys it `(module)` because no `fn` declaration precedes it. | Nothing executes. `Option<fn>` is the null-optimised representation C's nullable function pointer needs, and `#[repr(C)]` on the struct is what makes the field's position an ABI fact rather than an implementation detail. **This crate never invokes the callback**, so the `*mut i16` coefficient block it would receive has no producer here. | no: C ABI — a field a C caller fills in. | **none** — no suite sets `custom_filter`, so the field is carried for ABI parity and never called. `abi_offsets` (tests/abi_offsets.rs) pins the struct's layout. | *CAPI ABI Checks* (three OSes) for the layout. Nothing executes it, because nothing can. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3Transform` | 5 | Declaration; `from_raw_parts` over the source JPEG; a second over the caller's `n`-entry `transforms` array; a block taking `dst_bufs.add(i)` and `dst_sizes.add(i)` per output and handing them to `deliver_compressed_output`; and `with_handle`. | `jpeg_buf` non-NULL with `jpeg_size >= 2`, `n > 0`, and all three arrays non-NULL are checked before any slice is formed. `add(i)` is in bounds for `i < n` on the caller's assertion that the arrays have `n` slots — the one fact nothing here can verify. Each slot honours `TJPARAM_NOREALLOC` **independently**, which is what a caller pre-sizing an array of destinations relies on; before P4-145 they were freed unconditionally, with the wrong allocator whenever they were not `malloc`-owned. `TJXOPT_NOOUTPUT` skips delivery entirely rather than zeroing a slot's size. | no: C ABI. | `tj3_transform_runs_multiple_ops_in_one_call` and `tj3_transform_validates_arguments` (tests/transform.rs); `tj3_transform_keeps_the_callers_destination_buffers` and `transform_with_no_output_needs_no_destination` (tests/norealloc_all_entry_points.rs, traced against the C oracle). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3TransformBufSize` | 3 | Declaration; `&*transform` to read the caller's `tjtransform` by reference; and `with_handle`. | `transform` is NULL-checked before the reference is formed; the struct is `#[repr(C)]` plain data, so reading it needs only that it be valid and aligned. Dimensions come from the handle (`tj3DecompressHeader` must have run, else 0 and an error). The bound adds the stored ICC length to `tj3JPEGBufSize`, saturating so it can never return **less** than the geometry-only bound — and deliberately *not* shared with the legacy `tjTransform` bridge, which must not add metadata to a capacity a caller derived from `tjTransformBufSize` (P4-151: an 8192-byte destination against a 139264-byte capacity at 32x32 with a 128 KiB profile). | no: C ABI. | `tj3_transform_buf_size_includes_icc_size` (tests/precision.rs); `legacy_tj_transform_fills_a_zero_dst_size_from_geometry` and `legacy_tj_transform_sizes_a_grayscale_source_as_gray` (tests/norealloc_all_entry_points.rs) pin the separation from the legacy path. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |

## `crates/libjpeg-turbo-rs-capi/src/yuv.rs` — 32 sites

| Item | Sites | Why not safe Rust | Invariant | Safe caller | Regression test | Tool coverage | Reviewer |
|---|---|---|---|---|---|---|---|
| `densify_pitched_bytes` | 1 | One `slice::from_raw_parts(src.add(row * line), row_bytes)` per row, in a **safe `fn`** that takes `*const u8`. Reading row by row rather than as one span is the point: the trailing pad of the last row may not be allocated, so a single `from_raw_parts` over `pitch * height` would claim memory the caller never owned. | `pitch < 0` and `pitch < width * bpp` are both rejected before any read, so `line >= row_bytes` and the maximum byte touched is `(height - 1) * line + row_bytes - 1` — exactly libjpeg-turbo's `srcBuf` contract, and the reason `tj_encode_yuv3_does_not_over_read_padded_input_buffer` exists. `row_bytes * height` for the `Vec` capacity is an unchecked product; it sizes a Rust allocation rather than a raw span, so an overflow aborts rather than mis-sizing a slice. **This is a safe `fn` whose first argument is a raw pointer — the P4-135 shape.** It is private to this module and both callers are `unsafe extern "C" fn`s, so nothing outside can reach it, but the signature itself promises nothing. | no: private safe `fn` taking a raw pointer, called only by `tj3EncodeYUV8` and `tj3EncodeYUVPlanes8`. | `tj_encode_yuv3_does_not_over_read_padded_input_buffer` (tests/legacy_aliases.rs) is the direct regression: it places the source at the end of a page-sized allocation so an over-read past the last row would fault. `tj3_yuv_full_pipeline_round_trips_rgb_444` (tests/yuv.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness — so the over-read guard rests on that one hand-built fixture rather than on a sanitizer. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3EncodeYUV8` | 3 | Declaration; `copy_nonoverlapping` of the packed planes into the caller's `dstBuf`; and `with_handle`. The source pixels are read through the safe `densify_pitched_bytes`. | `src_buf`/`dst_buf` non-NULL, positive dimensions, and `align >= 1` and a power of two are all checked at entry — upstream's order (`turbojpeg.c:1754-1759`), with the pixel-format check downstream in the planar delegate. The destination is the caller's `tjBufSizeYUV2(width, align, height, subsamp)` buffer, which sizes `current_plane_count(inst)` planes; `packed.len()` covers `emitted <= plane_count` planes, so the copy cannot exceed it. The one case where the two differ is a grayscale *source* under a non-grayscale subsampling, where this **under**-writes rather than over-writes — a divergence from upstream, which refuses the combination, tracked as P4-166. | no: C ABI. | `case_encode_yuv8` and `packed_gray_encode_writes_only_the_luma_plane` (tests/capi_yuv_gray.rs, several cases traced against stock TurboJPEG in `gray_yuv_entry_points_match_stock_turbojpeg`); `tj3_yuv_full_pipeline_round_trips_rgb_444` and `tj3_yuv_api_rejects_bad_pointers` (tests/yuv.rs); `unset_encodeyuv8_case` (tests/capi_compress_precision.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3EncodeYUVPlanes8` | 3 | Declaration; one block that reads `*dst_planes.add(i)` and `*strides.add(i)` and `copy_nonoverlapping`s each plane row into the caller's plane; and `with_handle`. | The loop runs `emitted = current_plane_count(inst).min(planes.len())` times, not three: **GRAY writes plane 0 only** (P4-165), because upstream's packed wrapper passes NULL for slots 1 and 2 and its NULL check exempts GRAY, so a caller may legally supply a one-element array and walking three slots reads past it. Each `dst` is NULL-checked inside the loop; `strides` may itself be NULL, in which case the plane width is used. Row `row` writes `pw` bytes at `dst + row * stride`, on the caller's assertion that each plane holds `stride * plane_height` samples. | no: C ABI. | `case_encode_yuv_planes8` and `planar_gray_encode_touches_only_plane_zero` (tests/capi_yuv_gray.rs), the latter comparing untouched-slot digests; `tj3_yuv_api_rejects_bad_pointers` (tests/yuv.rs); `unset_encodeyuvplanes8_case` (tests/capi_compress_precision.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3CompressFromYUV8` | 4 | Declaration; `from_raw_parts` over the caller's packed YUV buffer; `deliver_compressed_output`; and `with_handle`. | The length is `packed_yuv_len(width, height, align, ss, plane_count)` with `plane_count = current_plane_count(inst)` — **one** plane under GRAY, which is what `tj3YUVBufSize` promised the caller. Reading three from a buffer sized for one ran the slice past the allocation (P4-165). The helper is checked arithmetic bounded by `isize::MAX`, and returns `None` on overflow rather than a wrapped length. | no: C ABI. | `case_compress_from_yuv8`, `gray_compress_from_yuv_emits_a_one_component_jpeg` and `packed_and_planar_three_plane_compress_agree` (tests/capi_yuv_gray.rs); `tj3_compress_from_yuv8_rejects_packed_length_overflow` (tests/yuv_packed_length_overflow.rs); `tj3_compress_from_yuv8_keeps_the_callers_buffer` (tests/norealloc_all_entry_points.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3CompressFromYUVPlanes8` | 4 | Declaration; a block reading `*src_planes.add(c)`, `*strides.add(c)` and a `from_raw_parts` per plane row; `deliver_compressed_output`; and `with_handle`. | The loop runs `current_plane_count(inst)` times, one under GRAY for the same P4-165 reason. Each plane pointer is NULL-checked before its rows are read; a NULL or non-positive stride falls back to the plane width. Each row read is `pw` bytes at `plane_ptr + row * stride`, on the caller's assertion. | no: C ABI. | `case_compress_from_yuv_planes8` and `packed_and_planar_gray_paths_agree` (tests/capi_yuv_gray.rs); `tj3_compress_from_yuv_planes8_keeps_the_callers_buffer` (tests/norealloc_all_entry_points.rs); `unset_fromyuvplanes8_case` (tests/capi_compress_precision.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3DecompressToYUV8` | 4 | Declaration; `from_raw_parts` over the caller's JPEG; `copy_nonoverlapping` of the packed planes into `dstBuf`; and `with_handle`. | Every argument is validated **at entry, before the header is read** — `jpeg_buf`/`dst_buf` non-NULL, `jpeg_size >= 2`, `align` a positive power of two. `align` used to be discovered inside `pack_yuv_planes`, i.e. *after* the component guard, which flipped the precedence C reports (P4-127). The component count is then bounded from the *header* rather than after a full decode, so `TJPARAM_MAXPIXELS` applies. The packed planes are clamped to `MAX_YUV_PLANES` a second time before the copy, because `planes` comes from an independent second parse: the two parses agreeing is an invariant of this crate, not of the caller's three-plane buffer. | no: C ABI. | `four_component_frame_is_rejected_without_decoding`, `max_pixels_bounds_the_packed_entry_point`, `bad_align_outranks_the_component_check_as_in_c` (tests/yuv_validate_before_decode.rs); `tj3_decompress_to_yuv8_rejects_four_component_jpeg` (tests/yuv_four_component_guard.rs); `gray_decompress_to_yuv_emits_only_the_luma_plane` (tests/capi_yuv_gray.rs); `yuv_decompress_component_count_matches_c` (tests/yuv_four_component_c_parity.rs, skipped without a TurboJPEG 3 install). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3DecompressToYUVPlanes8` | 5 | Declaration; the JPEG slice; a pre-pass reading `*dst_planes.add(i)` for the NULL check; the copy loop that re-reads the plane pointer, reads `*strides.add(i)` and `copy_nonoverlapping`s each row; and `with_handle`. | The NULL-plane pre-pass is the extra site and the reason for it: upstream rejects a NULL chroma plane up front, so nothing is written when it fails — checking inside the copy loop meant planes 0 and 1 were already in caller memory by the time a NULL plane 2 was noticed. The pre-pass runs `info.num_components.min(MAX_YUV_PLANES)` times from the *header*; the copy loop takes `MAX_YUV_PLANES` from the second parse, for the same two-parses reason as the packed sibling. `dstPlanes` is a three-element array in `turbojpeg.h`, which is what `MAX_YUV_PLANES` encodes. | no: C ABI. | `null_plane_pointer_leaves_all_caller_buffers_untouched` (tests/yuv_validate_before_decode.rs) is the direct regression for the pre-pass; `tj3_decompress_to_yuv_planes8_rejects_four_component_jpeg` (tests/yuv_decompress_planes_component_guard.rs and tests/yuv_four_component_guard.rs); `case_decompress_to_yuv_planes8` and `planar_gray_decode_ignores_the_chroma_slots` (tests/capi_yuv_gray.rs). | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3DecodeYUV8` | 4 | Declaration; `from_raw_parts` over the caller's packed YUV source; the block writing each output row to `dst_buf + row_index * dst_stride`; and `with_handle`. | The source length is `packed_yuv_len(w, h, align, ss, current_plane_count(inst))` — one plane under GRAY, since reading three both ran the slice past the allocation and folded whatever followed it into the image as chroma (P4-165). Arguments are validated at entry in upstream's order. The destination stride is `pitch` when positive and `w * bpp` when zero, with `pitch < w * bpp` rejected; under `TJPARAM_BOTTOMUP` row `i` goes to `h - 1 - i`, so the set of bytes written is the same either way. Each row writes `w * bpp` bytes, on the caller's assertion that `dst` holds `dst_stride * h`. | no: C ABI. | `case_decode_yuv8`, `packed_gray_decode_ignores_bytes_past_the_luma_plane` and `case_decode_yuv8_three_plane` (tests/capi_yuv_gray.rs); `tj3_decode_yuv8_rejects_packed_length_overflow` (tests/yuv_packed_length_overflow.rs); `tj_yuv_legacy_aliases_actually_flip_rows_under_bottomup` (tests/legacy_aliases.rs) covers the bottom-up write order. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
| `tj3DecodeYUVPlanes8` | 4 | Declaration; a block reading `*src_planes.add(c)` and `*strides.add(c)` and a `from_raw_parts` per plane row; the block writing each output row to `dst_buf + row_index * dst_stride`; and `with_handle`. | `current_plane_count(inst)` planes, one under GRAY — which also selects `decode_yuv_planes`' grayscale path, writing R = G = B = Y, what YCbCr to RGB collapses to at Cb = Cr = 128. Each plane pointer is NULL-checked; a NULL or non-positive stride falls back to the plane width. The destination rules are `tj3DecodeYUV8`'s, including the bottom-up row mapping. | no: C ABI. | `case_decode_yuv_planes8` and `planar_gray_decode_ignores_the_chroma_slots` (tests/capi_yuv_gray.rs); `unset_decodeyuvplanes8_case` (tests/capi_compress_precision.rs); `tj_encode_decode_yuv_legacy_aliases_roundtrip_444` (tests/legacy_aliases.rs) through the legacy alias. | Integration Tests, both oracle legs. Not ASan/UBSan, not Miri, not ARMv7, not the C-boundary harness. | P4-141 c4 capi chunk, 2026-09-08 (source read; no independent human review yet) |
