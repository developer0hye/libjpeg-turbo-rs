# Fuzz Smoke Runs 117, 118, and 121 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make malformed 12-bit JPEG sampling layouts return a decoder error instead of panicking, and prove the fix against Fuzz Smoke runs 117, 118, and 121.

**Architecture:** Validate the sampling-factor invariants required by the current 12-bit MCU/upsampling implementation before allocating or decoding planes. Non-zero JPEG sampling layouts that do not evenly divide the maximum are legal but unsupported by this 12-bit path, so they return `Unsupported`; this does not change the separately tracked 8-bit/non-standard sampling behavior. Retain a write-boundary check as defense in depth, and pin behavior with the minimized CI input plus direct replay of all three downloaded artifacts.

**Tech Stack:** Rust 2021, cargo test, cargo-fuzz/libFuzzer, GitHub Actions artifacts

---

## Chunk 1: Reproduction and fix

### Task 1: Preserve CI evidence and reproduce the failures

**Files:**
- Read: `/tmp/libjpeg-fuzz-runs-117-118-121/{117,118,121}`
- Read: `src/api/precision.rs`

- [x] Download `fuzz-repro-*` and `fuzz-artifacts-*` for runs 117, 118, and 121 outside the repository.
- [x] Read each `repro.txt` and `versions.txt`.
- [x] Replay all three artifacts at failing commit `6500749` and record the common panic at the 12-bit plane write.
- [x] Replay all three artifacts with the candidate fix and verify that none panic.
- [x] Read `CLAUDE.md`, `docs/LAST_MILE.md`, `docs/last_mile/phase4.md`, `METHODOLOGY.md`, and `PRD.md` if present.

### Task 2: Add the regression before finalizing the implementation

**Files:**
- Modify: `src/api/precision.rs`

- [ ] Add a minimized 12-bit malformed-sampling fixture to the precision decoder tests.
- [ ] Assert that `decompress_12bit` returns an error rather than panicking.
- [ ] Run `cargo test malformed_12bit_scan_block_outside_component_plane_returns_error -- --exact` against the original implementation and confirm failure.

### Task 3: Implement general validation

**Files:**
- Modify: `src/api/precision.rs`

- [ ] Reject zero factors as corrupt and legal non-integral-divisor layouts as unsupported by the current 12-bit upsampler before MCU sizing.
- [ ] Check every decoded 8x8 block fits its component plane before writing samples.
- [ ] Run the focused regression test and the precision/API test suite.
- [ ] This regression asserts safe error handling and produces no decoded pixels, so the decode-output C cross-validation rule is not applicable; verify separately that stock `djpeg` does not crash on the CI artifacts when a compatible 12-bit build is available.

## Chunk 2: Project tracking and verification

### Task 4: Record and close the discovered gap

**Files:**
- Modify: `docs/last_mile/phase4.md`
- Modify: `docs/LAST_MILE.md`

- [ ] Add P4-30 as closed with the three run numbers, root cause, implementation, and proof.
- [ ] Keep the LAST_MILE index concise and update Phase 4 history/status.
- [ ] Confirm README and API documentation need no update because behavior only changes malformed-input failure handling.

### Task 5: Verify the complete fix

**Files:**
- Test: `src/api/precision.rs`
- Test inputs: `/tmp/libjpeg-fuzz-runs-117-118-121/*/fuzz-artifacts-*/*`

- [ ] Run `cargo fmt --all` and `cargo fmt --all -- --check`.
- [ ] Run the focused regression and relevant crate tests.
- [ ] Replay all three artifacts through their original fuzz targets with `-runs=1`.
- [ ] Run `cargo test` and `cargo clippy --lib -- -D warnings`.
- [ ] Review the final diff, ensuring existing untracked fuzz corpus is untouched.
- [ ] Run the required code-reviewer and `codex review --uncommitted` before committing; apply valid findings and repeat focused/full tests after changes.
- [ ] Run `cargo test`, create a signed commit on a focused fix branch, and push it.
- [ ] Open a PR and watch all required GitHub Actions checks until green before reporting completion.
