[package]
name = "libjpeg-turbo-rs-capi"
version = "0.1.0"
edition = "2021"
rust-version = "1.87"
description = "C ABI shim for libjpeg-turbo-rs: drop-in replacement for libjpeg.so.62 and libturbojpeg.so.0"
license = "MIT OR Apache-2.0"
repository = "https://github.com/developer0hye/libjpeg-turbo-rs"
publish = false
build = "build.rs"

[lib]
name = "libjpeg_turbo_rs_capi"
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
libjpeg-turbo-rs = { path = "../.." }

[dev-dependencies]
libloading = "0.8"
tempfile = "3"
